<!-- res/drawable/edittext_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Прозрачный фон -->
    <stroke
        android:width="2dp"
        android:color="@color/purple" /> <!-- Цвет рамки -->
    <corners
        android:radius="8dp" /> <!-- Закругленные углы -->
</shape>